export const GuiaCards =({ icon, title, description })=>{
    return(
        <div className="GuiaCards">
        <div>
            <img src={checkIcon} alt="checkIcon" />
   </div>

    </div>

    )
}